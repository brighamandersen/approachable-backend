generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:../db.sqlite3"
}

model User {
  id                    Int       @id @default(autoincrement())
  firstName             String
  lastName              String
  birthDate             Int
  bio                   String?
  interestedInFriends   Boolean   @default(false)
  interestedInDating    Boolean   @default(false)
  interestingInBusiness Boolean   @default(false)
  interestedInHelp      Boolean   @default(false)
  latitude              Float
  longitude             Float
  locationLastUpdated   Int
}